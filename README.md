#racer-telemetry

Realtime telemetry graphs and manual driving overrides for rustbuggy.

#install
##mac os x
    brew install sdl2
    pip install pyopengl
    pip install pysdl2
    pip install pillow
